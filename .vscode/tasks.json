{
    "tasks": [
        {
            "type": "cargo",
            "command": "build",
            "options": {
                "cwd": "${workspaceRoot}",
            },
            "args": ["--target", "thumbv7m-none-eabi"],
            "problemMatcher": [
                "$rustc"
            ],
            "group": "build",
            "label": "cargo build"
        }
    ]
}